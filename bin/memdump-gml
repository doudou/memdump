#! /usr/bin/env ruby

require 'pathname'
require 'memdump/json_dump'
require 'memdump/convert_to_gml'

STDOUT.sync = true
dump = MemDump::JSONDump.new(Pathname.new(ARGV[0]))
io = File.open(ARGV[1], 'w') do |io|
    MemDump.convert_to_gml(dump, io)
end

